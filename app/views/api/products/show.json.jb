{
  id: @product.id,
    name: @product.name,
    price: @product.price,
    image_url: @product.image_url,
    description: @product.description,
    tax: @product.tax,
    discounted: @product.is_discounted?,
    total: @product.total
}